import CentralizeContainer from "@/components/Shared/CentralizeContainer";
import LoadingComponent from "@/components/Shared/LoadingComponent";

const Loading = () => {
  return (
    <CentralizeContainer>
      <LoadingComponent />
    </CentralizeContainer>
  );
};

export default Loading;
